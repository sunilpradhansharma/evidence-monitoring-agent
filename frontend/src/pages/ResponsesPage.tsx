import ComingSoon from "../components/common/ComingSoon";

export default function ResponsesPage() {
  return (
    <ComingSoon
      title="Responses"
      note="A filterable, paginated table of every captured response with search, filters, and CSV export — lands in the next stage."
    />
  );
}
