import ComingSoon from "../components/common/ComingSoon";

export default function ComparisonPage() {
  return (
    <ComingSoon
      title="LLM Comparison"
      note="Pick an approved question and read each model's answer side by side for a chosen run — lands in the next stage."
    />
  );
}
