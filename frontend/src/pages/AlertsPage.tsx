import ComingSoon from "../components/common/ComingSoon";

export default function AlertsPage() {
  return (
    <ComingSoon
      title="Alerts"
      note="A filterable alert feed with KPI tiles per alert type the engine actually produces — lands in the next stage."
    />
  );
}
