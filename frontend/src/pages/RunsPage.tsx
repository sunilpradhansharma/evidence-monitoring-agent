import ComingSoon from "../components/common/ComingSoon";

export default function RunsPage() {
  return (
    <ComingSoon
      title="Runs"
      note="A run-history table with capture, alerts, tokens, est. cost, and status — lands in the next stage."
    />
  );
}
